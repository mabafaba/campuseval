{
  "name": "campuseval",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker": "docker build -t campuseval . && docker compose up",
    "deploy": "git pull && docker build -t campuseval . && docker compose up"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.7.9",
    "express": "^4.21.2",
    "fs": "^0.0.1-security",
    "mongoose": "^7.8.3",
    "path": "^0.12.7"
  }
}
